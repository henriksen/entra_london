@page
@model Incremental.Pages.ReadModel
@{
}


<div>@Model.Token</div>

<div>
    @foreach (var claim in Model.JwtSecurityToken.Claims)
    {
        <div>@claim.Type: @claim.Value</div>
    }

</div>